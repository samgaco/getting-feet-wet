<h1>Users</h1>
<h3>Name          Email</h3>

  
<% @users.each do |user| %>
  <%= user.id %> <%= user.name%>   <%= user.email%>   <%= link_to 'show', user %> <%= link_to "Edit", edit_user_path(user.id) %> <%= link_to "Delete", user_path(user.id), method: :delete,data: {confirm: "Really delete the #{user.name}"} %>    
  <br>
<% end %>

<%= link_to 'new user' , new_user_path %>

